export default class Additionalinfo {
    constructor(phone_number, address, about_me) {
        this.phone_number = phone_number;
        this.address = address;
        this.about_me = about_me;
    }
}